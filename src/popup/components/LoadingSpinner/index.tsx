import React from "react";
import "./LoadingSpinner.css";

/**
 * Loading spinner component that displays a circular CSS animation.
 * Used to indicate loading/processing states in buttons and other UI elements.
 */
export const LoadingSpinner: React.FC = () => {
  return <div className="spinner" />;
};
